# CPU-only torch wheels are ~10x smaller than the default CUDA build and
# it's all we need (no GPU in this deployment target)
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
pydantic
python-dotenv

# LLM (free tier)
groq

# embeddings + reranking (local, free)
sentence-transformers
torch

# retrieval
qdrant-client
rank-bm25

# agent orchestration
langgraph
langchain-core

# ingestion
pypdf
beautifulsoup4
requests
playwright  # for JS-rendered source pages - see ingestion/fetch_corpus.py

# eval + observability
# (faithfulness scoring is implemented directly against Groq in eval/faithfulness.py -
# the `ragas` package's LLM factory unconditionally imports langchain_community's
# Vertex AI integration, which fails without the unrelated Google Cloud SDK installed)
arize-phoenix
arize-phoenix-otel
openinference-instrumentation-groq

# tracker
sqlalchemy
