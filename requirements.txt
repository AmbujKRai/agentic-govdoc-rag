# Runtime dependencies only - what the deployed API (api/main.py), the
# agent graph, retrieval, the eval harness, and the tracker actually import.
# Ingestion (PDF/HTML fetching+parsing) and the optional Phoenix tracing
# demo have their own extra dependencies that the deployed image doesn't
# need to pay the install-time/build-time cost for - see requirements-dev.txt.

# CPU-only torch wheels are ~10x smaller than the default CUDA build and
# it's all we need (no GPU in this deployment target)
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
pydantic
python-dotenv

# LLM (free tier)
groq

# embeddings + reranking (local, free)
sentence-transformers
torch

# retrieval
qdrant-client
rank-bm25

# agent orchestration
langgraph
langchain-core

# tracker
sqlalchemy
